在 E-Hentai / ExHentai 页面中，自动查询本地 `exhentai-manga-manager` 服务，并对本地已存在的本子做彩色高亮。

使用方式：
- 本地启动 `exhentai-manga-manager`
- 保证 API 地址 `http://localhost:23786/api/search?length=1000000` 可访问
- 打开 E-Hentai 或 ExHentai 页面
- 脚本会自动轮询并高亮匹配到的本子

当前默认高亮颜色为 `霓虹色`。
